// Backend client is temporarily disconnected.
// Recreate the real client here when backend work resumes.

export const backendDisabled = true;

console.info('[config.js] Backend connection is temporarily disabled.');
